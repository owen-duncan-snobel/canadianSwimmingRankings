import React from 'react';
import { render } from '@testing-library/react';
import ClubDashboard from '../../controllers/clubDashboard/clubDashboard';
import * as t from '../../test.json';
import 'jest-canvas-mock';

test('Checking if Clubs Renders correctly', () => {

    // const { clubing } = render(<ClubDashboard swimmerData={[t.default]} event={'50m Fr'} swimEvent={'all'} selectedData={[t.default[0]]}/>);
})
